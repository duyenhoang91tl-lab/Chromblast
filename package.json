{
  "name": "chromablast",
  "version": "1.0.0",
  "description": "ChromaBlast - color matching puzzle game",
  "private": true,
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "cap:sync": "npm run build:www && npx cap sync android"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.1.2",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
