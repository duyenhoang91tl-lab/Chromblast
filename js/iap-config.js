// RevenueCat Android SDK key (PUBLIC key — an toàn để commit, không phải secret key).
// Lấy tại: RevenueCat Dashboard → Project → API keys → Android (Google Play).
// Xem hướng dẫn đầy đủ trong js/iap.js (đầu file).
window.REVENUECAT_ANDROID_KEY = 'REPLACE_WITH_REVENUECAT_ANDROID_KEY';
