node_modules/
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/
*.keystore
*.jks
key.properties
# Cấu hình cục bộ của công cụ
.claude/

# Bản clone repo lồng trong workspace (không thuộc source game)
# Lưu ý: có dấu / ở đầu để chỉ khớp thư mục gốc, tránh trùng với
# đường dẫn package Android android/app/src/main/java/.../chromblast/
/Chromblast/
/chromblast/
/pulse/
/www/
